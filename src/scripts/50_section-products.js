'use strict';

/* $(function () {
  $('#myTab li:last-child a').tab('show');

  $('a[data-toggle="tab"]').on('shown.bs.tab', function (e) {
    e.target;
    e.relatedTarget;
  });
})

(function() {
   var tabs = document.getElementById('furnitureTab');

   tabs.addEventListener('click', function() {

   }

   var buttonClickCallback = function(event){
       event.preventDefault();

       var checkTag = this.parentNode;
       while(checkTag.tagName != 'SECTION'){
           if(checkTag == document.body){
               return;
           }
           checkTag = checkTag.parentNode;
       }

       checkTag.classList.toggle('green');
   };

   var buttons = document.querySelectorAll('.toggle-section.vanillajs');

   for(var i = 0; i < buttons.length; i++){
       buttons[i].addEventListener('click', buttonClickCallback);
   }
);

*/
